spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/starter
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    jdbc:
      initialize-schema: always

logging:
  level:
    org.springframework.batch: DEBUG
    fr.rawz06.starter: DEBUG
